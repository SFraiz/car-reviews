<h1>Users</h1>

<ul class="users-list">
<% @users.each do |user| %>
	<li>
		<%= image_tag user.profile_picture.url(:thumb) %> <%= user.nickname %> - <%= link_to 'Profile', user_path(user.id) %>
	</li>
<% end %>
</ul>